namespace PluginManagerObs.Models
{
    public enum PluginInstallationType
    {
        NOT_INSTALLED,
        INSTALLED,
        INSTALLED_MODIFIED,
        FILES_PRESENT,
        MANUALLY_INSTALLED
    }
    public enum PluginInstallationStateType
    {
        NOT_INSTALLED,
        INSTALLED,
        FILES_PRESENT
    }
}
